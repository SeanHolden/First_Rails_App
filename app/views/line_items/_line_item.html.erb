<tr>
	<%# the variable used has to be same name as the file, in this case 'line_item' %>
  <td><%= line_item.quantity %>&times;</td>
  <td><%= line_item.product.title %><br />
  	<%= button_to 'Remove', line_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  <td class="item_price"><%= number_to_currency(line_item.total_price, :unit => "£") %></td>
</tr> 